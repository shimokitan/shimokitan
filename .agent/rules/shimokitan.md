---
trigger: always_on
---

* **Stack:** Bun, Cloudflare, Monorepo.
* **Database:** Neon (Project: `shimokitan` only).
* **UI:** `shadcn/ui`. Use high-contrast colors and readable fonts (min 14px).
* **Architecture:** Shared-first; move logic to `@shared` immediately.
* **i18n:** Use translation keys for all user-facing strings.
* **Tone:** Balance "Vibey" (creative/modern) with "Professional" (robust/standard).
* **Maintenance:** Audit existing packages before adding new ones.